class OrderBook {
  
  constructor (){ 
    this.orders = new Array();
  }


  // WIP
  addOrder(order) {
    console.log('adding order: ', order);
    // do stuff;
    return;
  }
  
};

module.exports = {
  OrderBook,
}